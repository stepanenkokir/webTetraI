import React, {useEffect, useState} from 'react'

export const MLATTargets = ()=>{
    const [mlat, setMlat] = useState([]);  
    return mlat;
}

export const ADSBTargets = ()=>{
    const [adsb, setAdsb] = useState([]);  
    return adsb;
}
